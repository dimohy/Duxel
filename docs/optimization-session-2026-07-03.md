# Optimization Session — 2026-07-03

## Scope

GPU-driven renderer migration (plan: `docs/gpu-driven-renderer-plan.md`):

- Phase A: bindless textures (Vulkan 1.2 descriptor indexing).
- Phase B: unified dual-source-blend pipeline.
- Phase C: vertex pulling / single pipeline.
- Cleanup: obsolete path deletion + `VulkanRendererBackend` partial-class consolidation.

Baseline captured 2026-07-03 (managed quick gates, same machine/build):

- layer-widget quick (density 100, 0.9s): nocache `586.917`, cache `5762.815` FPS (`artifacts\bindless-baseline-layer-widget-quick.json`).
- global quick (density 1600, 0.4s): all-dynamic `981.247`, static-cache `1933.467` FPS (`artifacts\bindless-baseline-global-quick.json`).

## Changes and Measurements

| Target | Summary | Validation Command | Before | After | Improvement | Risk / Follow-up |
|---|---|---|---|---|---|---|
| Phase A — Bindless textures | One global `sampler2D[]` descriptor array (capacity 4096, UPDATE_AFTER_BIND + PARTIALLY_BOUND + UPDATE_UNUSED_WHILE_PENDING) bound once per frame. Texture ID → slot allocator with free-list reuse; slots recycled at the deferred texture-destroy point. Per-texture descriptor sets, the 11-type descriptor pool, and per-command `vkCmdBindDescriptorSets` deleted. Fragment shaders (`imgui.frag`, `imgui_subpixel.frag`, `primitive.frag`) index the array with a fragment-stage push constant (offset 20, 4 bytes); vertex push range (scale/translate/opacity, 20 bytes) unchanged. Device creation now requires `shaderSampledImageArrayDynamicIndexing`, `descriptorBindingSampledImageUpdateAfterBind`, `descriptorBindingUpdateUnusedWhilePending`, `descriptorBindingPartiallyBound`, `runtimeDescriptorArray` and fails explicitly when missing. | `dotnet build Duxel.slnx -c Release`; `glslangValidator -V --target-env vulkan1.2` + `spirv-val --target-env vulkan1.2` on 3 shaders; managed layer-widget/global quick gates; capture script visual check | layer-widget quick: nocache `586.917`, cache `5762.815`. Global quick: `981.247` / `1933.467`. | layer-widget quick: nocache `585.530`, cache `6833.921`. Global quick r1/r2/long: `1754.839`/`1821.315`/`4856.120` static-cache (long run used 1.2s phases; quick runs vary ±7% run-to-run). Capture-scene cache-170 run recorded `8102.603` FPS. | Mixed cache `+18.59%`; nocache `-0.24%` (noise). Global gate within run noise. Visual capture `captures\bindless-phaseA-layer-widget.png` confirmed correct text/layer/texture rendering vs 2026-05-16 reference. | UPDATE_AFTER_BIND descriptor memory can be marginally slower on some architectures; NVIDIA gate showed no regression beyond noise. Devices without descriptor indexing now fail explicitly (no fallback by policy). Bindless capacity fixed at 4096 slots; exceeding it throws. |
| Phase B — Unified dual-source-blend pipeline | All draws now use ONE blend state (`One/OneMinusSrc1Color`, `One/OneMinusSrc1Alpha`): shaders output premultiplied color plus a per-channel blend factor. Standard draws emit `blendFactor = vec4(alpha)` (mathematically identical to `SrcAlpha/OneMinusSrcAlpha`); ClearType subpixel text keeps per-channel coverage, selected by the high bit of the fragment push-constant texture index. Deleted: `imgui_color.frag`, `imgui_subpixel.frag`, `primitive_color.frag`, `solid.vert`, `_graphicsColorPipeline`, `_solidColorPipeline`, `_subpixelPipeline`, `_primitiveColorPipeline`, triangle-color/solid-unified device-policy modes (`DUXEL_VK_TRIANGLE_COLOR_PIPELINE`, `DUXEL_VK_SOLID_UNIFIED_PIPELINE`, `DUXEL_VK_SOLID_UNIFIED_STATIC`), the solid-triangle primitive sentinel infrastructure, and dead pipeline stats. Two pipelines remained after this phase (triangle + primitive). All commands resolve a bindless texture slot; solid commands sample the app 1x1 white texture. Static primitive triangle expansion no longer depends on the removed color pipeline. | `dotnet build Duxel.slnx -c Release` (0 warn/0 err); shader compile+`spirv-val`; managed layer-widget/global gates; `DUXEL_VK_PROFILE=1` bind profile; capture script visual check | Post-Phase-A: layer-widget quick nocache `585.530` / cache `6833.921`; global long static-cache `4856.120`. 2026-06-05 profile: `binds(pipe=47 ...)`, transitions `font=22 tri2prim=22 prim2tri=21`. | layer-widget quick: nocache `738.089` / cache `7437.032`. Global long: all-dynamic `1517.919` / static-cache `5330.070`. Profile: `binds(pipe=20 desc→texIndex-push=40 geom=7 prim=1)`, `pipeClass(font=4 texTri=6 texPrim=10)`, transitions `tri2prim=12 prim2tri=13` (font pipeline alternation eliminated). | nocache `+26.05%`, mixed cache `+8.82%`, global static-cache `+9.76%`. Pipeline binds `-57%` in the profiled frame. Visual capture `captures\unified-phaseB-layer-widget.png` matches Phase A reference. | Dual-source blending may disable blend fast paths on some GPUs — NVIDIA gate improved; re-gate on AMD/Intel when hardware is available. Remaining pipeline churn was triangle↔primitive only (removed by Phase C). |
| Phase C — Vertex pulling, single pipeline | ONE graphics pipeline for all UI draws. Vertex/primitive-instance data is read in the vertex shader through buffer device addresses (`GL_EXT_buffer_reference`); the pipeline has no vertex input state. Push constant layout: vertex range [0,40) = scale/translate/opacity/drawMode + vertex-buffer address + primitive-buffer address; fragment range [40,44) = texIndexAndMode. Draw mode (indexed triangles vs primitive expansion) switches via a 4-byte push instead of pipeline binds; `vkCmdBindPipeline` happens once per frame. `vkCmdBindVertexBuffers` is gone; only index-buffer binds remain. Deleted: `primitive.vert`, `primitive.frag`, `_primitivePipeline`, `CommandPipelineSelection`, all vertex-input state. Device now also requires `bufferDeviceAddress` (explicit failure otherwise). First candidate (host-visible dynamic buffers) regressed nocache `738→498` because shader BDA loads crossed PCIe per vertex; retained fix allocates dynamic vertex/primitive buffers from BAR memory (`DEVICE_LOCAL|HOST_VISIBLE|HOST_COHERENT`) when the device exposes it, with plain host-visible as the stated required set. | `dotnet build Duxel.slnx -c Release` (0 warn/0 err); `glslangValidator`+`spirv-val` vulkan1.2; managed layer-widget/global gates; `DUXEL_VK_PROFILE=1`; capture script visual check; NativeAOT publish gate | Post-Phase-B: layer-widget quick nocache `738.089` / cache `7437.032`; global long `1517.919`/`5330.070`. Profile `stateUs(pipe=3.000)`, real pipeline binds 20/frame. | Layer-widget 1.8s: nocache `848.492` / cache `8675.357`. Global long r1/r2: `1415.259`/`4744.408` and `1615.083`/`5367.643`. Profile: one real `vkCmdBindPipeline` per frame (pipe=20 counter now counts 4-byte drawMode pushes), `stateUs(pipe=0.500)`. | nocache `+14.96%`, mixed cache `+16.65%`, global within noise band (r2 above Phase B). Host-visible-only first attempt was `-32%` nocache and was replaced, not retained. Visual capture `captures\vertexpull-phaseC-layer-widget.png` matches references. NativeAOT publish gate passed. | BAR (ReBAR) memory is preferred-not-required: devices without it fall to the stated host-visible requirement and may show the slower pull path — gate on such hardware before shipping defaults. Vertex-pulling cost on AMD/Intel iGPUs unmeasured. |
| Renderer module consolidation | `VulkanRendererBackend` partial-class files consolidated from 55 to 19 coherent modules (root/lifecycle, Device, Swapchain, PipelineResources, Resources, TextureResources, UploadScheduler, DynamicGeometry, StaticGeometry, StaticGeometryStorage, StaticPrimitivePolicy, Frame, CommandRecording, CommandState, CommandDraw, CommandScheduling, CommandDiagnostics, Diagnostics, Types). Deleted the unreferenced `fxaa.frag` shader pair; the shader directory now contains exactly one vertex and one fragment shader. | `dotnet build Duxel.slnx -c Release` (0 warn/0 err); long mixed gate; capture script visual check; NativeAOT publish gate | 55 `VulkanRendererBackend.*.cs` files, many single-method; 4 shader files incl. unused FXAA. | 19 renderer modules; 2 shader files (`imgui.vert`, `imgui.frag`). Final long mixed gate (`artifacts\gpu-driven-final-layer-widget-long.json`): nocache-100 `1076.570`, cache-100 `6147.882`, nocache-170 `735.653`, cache-170 `6235.832`. Final capture `captures\gpu-driven-final-layer-widget.png` correct; NativeAOT publish gate passed. | Structural: renderer file count `-65%`; long gate within the Phase C noise band (no regression). | Merge tooling initially dropped the root file's `IRendererBackend` interface declaration; fixed and gated. Future partial additions should extend the existing 19 modules instead of adding new single-method files. |

## Cumulative Session Result (2026-06-05 defaults → GPU-driven renderer)

- Mixed layer-widget quick cache gate: `5762.815` → `7130`–`8675` FPS band (`+24%` to `+50%`, run-dependent).
- Mixed layer-widget quick nocache gate: `586.917` → `836`–`848` FPS (`+42%` to `+45%`).
- Per-frame render state: 6 pipelines → **1 pipeline bound once per frame**; per-texture descriptor sets → **one bindless set bound once per frame**; `vkCmdBindVertexBuffers` eliminated; per-draw variation is now push constants + dynamic scissor only.
- Renderer sources: 55 partial files → 19 modules; shaders: 9 files → 2 files.

## Phase D — Dynamic Rendering (same session, post-cumulative)

| Target | Summary | Validation Command | Before | After | Improvement | Risk / Follow-up |
|---|---|---|---|---|---|---|
| VK_KHR_dynamic_rendering conversion | Render pass and framebuffer objects deleted. Frames record `vkCmdBeginRenderingKHR`/`vkCmdEndRenderingKHR` with `RenderingAttachmentInfo`; MSAA uses `resolveMode = AVERAGE` inline (MSAA target → swapchain resolve). Explicit per-frame image barriers replace implicit render-pass transitions: `UNDEFINED→COLOR_ATTACHMENT` before rendering (swapchain + MSAA image), `COLOR_ATTACHMENT→PRESENT_SRC` after. The single pipeline chains `PipelineRenderingCreateInfo` (swapchain format) with `RenderPass = null`. `VK_KHR_dynamic_rendering` (+ dependencies `VK_KHR_depth_stencil_resolve`, `VK_KHR_create_renderpass2`) added to required device extensions and the `dynamicRendering` feature to the explicit-failure gate; entry points loaded via `vkGetDeviceProcAddr` (`KhrDynamicRendering`). Deleted unused render-pass/framebuffer P/Invoke bindings, structs (`AttachmentDescription`, `SubpassDescription`, `SubpassDependency`, `RenderPassCreateInfo`, `RenderPassBeginInfo`, `FramebufferCreateInfo`...), and `SubpassContents`. | `dotnet build Duxel.slnx -c Release` (0 warn/0 err); managed layer-widget gates; capture scripts (1x + 4x MSAA); NativeAOT publish gate | Render pass + per-swapchain-image framebuffers created/destroyed on every swapchain (re)creation; pipeline bound to render pass object. Post-consolidation gate: nocache `1076.570` / cache `6147.882` (2.2s). | No render pass/framebuffer objects. 1.8s gate: nocache `966.585` / cache `8445.414` (within the established noise band). Visual captures `captures\dynrender-phaseD-layer-widget.png` (1x) and `captures\dynrender-phaseD-all-features-msaa4.png` (4x MSAA resolve, ClearType text) both correct. NativeAOT publish gate passed. | Structural: swapchain recreation path no longer rebuilds render-pass/framebuffer objects; perf-neutral within noise as expected. | Dynamic rendering requires `VK_KHR_dynamic_rendering` support (core in 1.3; extension on 1.2 desktop drivers — universally present on target GPUs). Devices without it now fail explicitly per no-fallback policy. New `capture-all-features.ps1` script retained for MSAA-path visual gates. |

## Verification

- `dotnet build Duxel.slnx -c Release` — succeeded, `0` warnings / `0` errors (exit 0).
- Shader validation — `glslangValidator -V --target-env vulkan1.2` and `spirv-val --target-env vulkan1.2` passed for `imgui.frag`, `imgui_subpixel.frag`, `primitive.frag`. Pre-change `.spv` backed up to `src/Duxel.Vulkan/_spv_backup/`.
- Managed layer-widget quick gate after Phase A — `artifacts\bindless-phaseA-layer-widget-quick.json`: nocache `585.530`, cache `6833.921` FPS.
- Managed global quick gates after Phase A — `artifacts\bindless-phaseA-global-quick.json` (`974.403`/`1754.839`), `artifacts\bindless-phaseA-global-quick-r2.json` (`913.456`/`1821.315`), long `artifacts\bindless-phaseA-global-long.json` (`1512.773`/`4856.120`).
- Visual verification — `captures\bindless-phaseA-layer-widget.png` compared against `captures\dynamic-classification-restored-layer-widget-mix.png` (2026-05-16 reference): same scene structure, correct text/texture/layer rendering.
